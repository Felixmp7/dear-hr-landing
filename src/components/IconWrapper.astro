---
interface Props {
    className?: string;
}
const { className = '' } = Astro.props;
const BASE_CLASS = 'bg-tertiary-500 h-10 md:h-12 md:p-3 md:w-12 p-2 rounded-full text-white w-10 flex justify-center items-center';
---

<div class:list={[BASE_CLASS, className]}>
    <slot />
</div>
